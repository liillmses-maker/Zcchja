local Players = game:GetService("Players")
local Player = Players.LocalPlayer
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local StarterGui = game:GetService("StarterGui")
local PLAYERGUI = Player:WaitForChild("PlayerGui")

local C = {
    BG = Color3.fromRGB(20,12,15),
    BG2 = Color3.fromRGB(30,18,23),
    PINK = Color3.fromRGB(255,170,195),
    WHITE = Color3.fromRGB(245,235,240),
    GRAY = Color3.fromRGB(160,140,150),
    ON = Color3.fromRGB(255,120,160),
    OFF = Color3.fromRGB(55,30,40),
    RED = Color3.fromRGB(255,40,60),
    YELLOW = Color3.fromRGB(255,200,100),
    PURPLE = Color3.fromRGB(180,75,255),
    CYAN = Color3.fromRGB(100,200,255),
    GREEN = Color3.fromRGB(100,255,100)
}

local Settings = {
    Fullbright = false,
    ShowNPC = false,
    ShowChanges = false,
    Speed = false,
    Fly = false,
    Noclip = false
}
local MasterOn = true

-- Simple notification
function Notify(txt)
    StarterGui:SetCore("SendNotification", {Title = "UWU HUB", Text = txt, Duration = 2})
end

-- Create GUI
local Gui = Instance.new("ScreenGui")
Gui.Name = "UwUHub"
Gui.Parent = PLAYERGUI
Gui.ResetOnSpawn = false

local Main = Instance.new("Frame")
Main.Name = "Main"
Main.Size = UDim2.new(0, 300, 0, 250)
Main.Position = UDim2.new(0.5, -150, 0.5, -125)
Main.BackgroundColor3 = C.BG
Main.BorderSizePixel = 0
Main.Active = true
Main.Draggable = true
Main.Visible = true
Main.Parent = Gui
Instance.new("UICorner", Main).CornerRadius = UDim.new(0, 14)

-- Header
local Header = Instance.new("Frame")
Header.Size = UDim2.new(1, 0, 0, 30)
Header.BackgroundColor3 = C.BG2
Header.BorderSizePixel = 0
Header.Parent = Main
Instance.new("UICorner", Header).CornerRadius = UDim.new(0, 14)

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(0, 150, 1, 0)
Title.Position = UDim2.new(0, 10, 0, 0)
Title.Text = "=^^= UWU HUB"
Title.TextColor3 = C.PINK
Title.Font = Enum.Font.GothamBold
Title.TextSize = 11
Title.BackgroundTransparency = 1
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = Header

-- Close button
local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 22, 0, 22)
CloseBtn.Position = UDim2.new(1, -28, 0.5, -11)
CloseBtn.Text = "X"
CloseBtn.BackgroundColor3 = C.OFF
CloseBtn.TextColor3 = C.WHITE
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.TextSize = 12
CloseBtn.BorderSizePixel = 0
CloseBtn.Parent = Header
Instance.new("UICorner", CloseBtn).CornerRadius = UDim.new(1, 0)
CloseBtn.MouseButton1Click:Connect(function()
    Main.Visible = false
    task.wait(0.5)
    Main.Visible = true
    Notify("Reopened!")
end)

-- Master toggle
local MasterBtn = Instance.new("TextButton")
MasterBtn.Size = UDim2.new(0, 50, 0, 22)
MasterBtn.Position = UDim2.new(1, -78, 0.5, -11)
MasterBtn.Text = "=^^= ON"
MasterBtn.BackgroundColor3 = C.ON
MasterBtn.TextColor3 = C.WHITE
MasterBtn.Font = Enum.Font.GothamBold
MasterBtn.TextSize = 8
MasterBtn.BorderSizePixel = 0
MasterBtn.Parent = Header
Instance.new("UICorner", MasterBtn).CornerRadius = UDim.new(0, 10)

MasterBtn.MouseButton1Click:Connect(function()
    MasterOn = not MasterOn
    if MasterOn then
        MasterBtn.Text = "=^^= ON"
        MasterBtn.BackgroundColor3 = C.ON
    else
        MasterBtn.Text = "=;= OFF"
        MasterBtn.BackgroundColor3 = C.OFF
    end
    Notify("Master: " .. (MasterOn and "ON" or "OFF"))
end)

-- Tabs
local TabBar = Instance.new("Frame")
TabBar.Size = UDim2.new(1, 0, 0, 22)
TabBar.Position = UDim2.new(0, 0, 0, 30)
TabBar.BackgroundColor3 = C.BG
TabBar.BorderSizePixel = 0
TabBar.Parent = Main

local tabs = {"Main", "ESP", "Player", "Log"}
local tabBtns = {}
local tabFrames = {}

for i, name in ipairs(tabs) do
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0.25, -2, 1, -2)
    btn.Position = UDim2.new((i-1)*0.25, 1, 0, 1)
    btn.Text = name
    btn.BackgroundColor3 = C.BG2
    btn.TextColor3 = C.GRAY
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 9
    btn.BorderSizePixel = 0
    btn.Parent = TabBar
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8)
    
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, -12, 1, -56)
    frame.Position = UDim2.new(0, 6, 0, 54)
    frame.BackgroundTransparency = 1
    frame.Visible = false
    frame.Parent = Main
    
    tabBtns[name] = btn
    tabFrames[name] = frame
    
    btn.MouseButton1Click:Connect(function()
        for n, b in pairs(tabBtns) do
            b.BackgroundColor3 = (n == name) and C.PINK or C.BG2
            b.TextColor3 = (n == name) and C.WHITE or C.GRAY
            tabFrames[n].Visible = (n == name)
        end
    end)
end

tabBtns["Main"].BackgroundColor3 = C.PINK
tabBtns["Main"].TextColor3 = C.WHITE
tabFrames["Main"].Visible = true

-- MAIN TAB
local mainFrame = tabFrames["Main"]

local infoFrame = Instance.new("Frame")
infoFrame.Size = UDim2.new(1, 0, 0, 50)
infoFrame.BackgroundColor3 = C.BG2
infoFrame.BorderSizePixel = 0
infoFrame.Parent = mainFrame
Instance.new("UICorner", infoFrame).CornerRadius = UDim.new(0, 10)

local infoText = Instance.new("TextLabel")
infoText.Size = UDim2.new(1, -10, 1, -10)
infoText.Position = UDim2.new(0, 5, 0, 5)
infoText.Text = "Anomaly Watch\n" .. Player.Name .. "\nUWU HUB HATE WORLD"
infoText.TextColor3 = C.WHITE
infoText.Font = Enum.Font.Gotham
infoText.TextSize = 10
infoText.BackgroundTransparency = 1
infoText.TextXAlignment = Enum.TextXAlignment.Left
infoText.TextYAlignment = Enum.TextYAlignment.Top
infoText.Parent = infoFrame

-- Cat
local catFrame = Instance.new("Frame")
catFrame.Size = UDim2.new(1, 0, 0, 110)
catFrame.BackgroundColor3 = C.BG2
catFrame.BorderSizePixel = 0
catFrame.Parent = mainFrame
Instance.new("UICorner", catFrame).CornerRadius = UDim.new(0, 10)

local catArt = Instance.new("TextLabel")
catArt.Size = UDim2.new(1, -6, 1, -6)
catArt.Position = UDim2.new(0, 3, 0, 3)
catArt.Text = [[   ╱|、
  ( -  -)  zZz
   |  ~ |
   じしˍ,)ノ
  =========]]
catArt.TextColor3 = C.PINK
catArt.Font = Enum.Font.Gotham
catArt.TextSize = 8
catArt.BackgroundTransparency = 1
catArt.TextXAlignment = Enum.TextXAlignment.Center
catArt.TextYAlignment = Enum.TextYAlignment.Center
catArt.Parent = catFrame

local catFrames = {
    [[   ╱|、
  ( -  -)  zZz
   |  ~ |
   じしˍ,)ノ
  =========]],
    [[   ╱|、
  ( -  -)  zzZ
   |  ~ |
   じしˍ,)ノ
  =========]],
    [[   ╱|、
  ( =  =)  Zzz
   |  ~ |
   じしˍ,)ノ
  =========]],
    [[   ╱|、
  ( ˘  ˘)  zZz
   |  ~ |
   じしˍ,)ノ
  =========]]
}
local catIdx = 1
spawn(function()
    while catArt and catArt.Parent do
        catIdx = catIdx % #catFrames + 1
        catArt.Text = catFrames[catIdx]
        wait(1.5)
    end
end)

-- Status
local StatusText = Instance.new("TextLabel")
StatusText.Size = UDim2.new(1, 0, 0, 16)
StatusText.Text = "=^^= Ready"
StatusText.TextColor3 = C.PINK
StatusText.Font = Enum.Font.GothamBold
StatusText.TextSize = 10
StatusText.BackgroundTransparency = 1
StatusText.TextXAlignment = Enum.TextXAlignment.Center
StatusText.Parent = mainFrame

-- TOGGLE FUNCTION
local function Toggle(parent, name, key, default)
    local enabled = Settings[key] or default
    
    local row = Instance.new("Frame")
    row.Size = UDim2.new(1, 0, 0, 28)
    row.BackgroundColor3 = C.BG2
    row.BorderSizePixel = 0
    row.Parent = parent
    Instance.new("UICorner", row).CornerRadius = UDim.new(0, 10)
    
    local lbl = Instance.new("TextLabel")
    lbl.Size = UDim2.new(0.5, 0, 1, 0)
    lbl.Position = UDim2.new(0, 8, 0, 0)
    lbl.Text = name
    lbl.TextColor3 = enabled and C.WHITE or C.GRAY
    lbl.Font = Enum.Font.Gotham
    lbl.TextSize = 10
    lbl.BackgroundTransparency = 1
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.Parent = row
    
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 48, 0, 24)
    btn.Position = UDim2.new(1, -54, 0.5, -12)
    btn.Text = enabled and "=^^= ON" or "=;= OFF"
    btn.BackgroundColor3 = enabled and C.ON or C.OFF
    btn.TextColor3 = C.WHITE
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 8
    btn.BorderSizePixel = 0
    btn.Parent = row
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 10)
    
    btn.MouseButton1Click:Connect(function()
        enabled = not enabled
        Settings[key] = enabled
        btn.Text = enabled and "=^^= ON" or "=;= OFF"
        btn.BackgroundColor3 = enabled and C.ON or C.OFF
        lbl.TextColor3 = enabled and C.WHITE or C.GRAY
        Notify(name .. ": " .. (enabled and "ON" or "OFF"))
    end)
    
    return row
end

-- ESP TAB
local espFrame = tabFrames["ESP"]
Toggle(espFrame, "Fullbright", "Fullbright", false)
Toggle(espFrame, "Show NPCs", "ShowNPC", false)
Toggle(espFrame, "Show Changes", "ShowChanges", false)

-- PLAYER TAB
local playerFrame = tabFrames["Player"]
Toggle(playerFrame, "Speed", "Speed", false)
Toggle(playerFrame, "Fly", "Fly", false)
Toggle(playerFrame, "Noclip", "Noclip", false)

-- LOG TAB
local logFrame = tabFrames["Log"]
local logBox = Instance.new("ScrollingFrame")
logBox.Size = UDim2.new(1, 0, 0, 170)
logBox.BackgroundColor3 = C.BG2
logBox.BorderSizePixel = 0
logBox.CanvasSize = UDim2.new(0, 0, 0, 0)
logBox.ScrollBarThickness = 3
logBox.ScrollBarImageColor3 = C.PINK
logBox.Parent = logFrame
Instance.new("UICorner", logBox).CornerRadius = UDim.new(0, 10)

local logLayout = Instance.new("UIListLayout")
logLayout.SortOrder = Enum.SortOrder.LayoutOrder
logLayout.Padding = UDim.new(0, 1)
logLayout.Parent = logBox

function AddLog(msg, color)
    local l = Instance.new("TextLabel")
    l.Size = UDim2.new(1, -4, 0, 13)
    l.Text = msg
    l.TextColor3 = color or C.WHITE
    l.Font = Enum.Font.Gotham
    l.TextSize = 8
    l.BackgroundTransparency = 1
    l.TextXAlignment = Enum.TextXAlignment.Left
    l.Parent = logBox
    logBox.CanvasSize = UDim2.new(0, 0, 0, logLayout.AbsoluteContentSize.Y + 10)
    logBox.CanvasPosition = Vector2.new(0, logBox.CanvasSize.Y.Offset)
end

AddLog("UWU HUB HATE WORLD", C.PURPLE)
AddLog("GUI Test - Working!", C.GREEN)
AddLog("Tabs: Main | ESP | Player | Log", C.CYAN)

Notify("GUI Loaded Successfully!")
