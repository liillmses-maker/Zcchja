-- UWU HUB HATE WORLD | PRO ESP ENGINE + SLEEPING CAT | FINAL v16

local Players = game:GetService("Players")
local Player = Players.LocalPlayer
local Camera = workspace.CurrentCamera
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local StarterGui = game:GetService("StarterGui")
local Lighting = game:GetService("Lighting")
local CollectionService = game:GetService("CollectionService")
local PLAYERGUI = Player:WaitForChild("PlayerGui")

local OrigBright = Lighting.Brightness; local OrigClock = Lighting.ClockTime
local OrigFog = Lighting.FogEnd; local OrigAmbient = Lighting.Ambient; local OrigOutdoor = Lighting.OutdoorAmbient
local function EnableFB() Lighting.Brightness=5; Lighting.ClockTime=12; Lighting.FogEnd=99999; Lighting.Ambient=Color3.new(1,1,1); Lighting.OutdoorAmbient=Color3.new(1,1,1); Lighting.GlobalShadows=false end
local function DisableFB() Lighting.Brightness=OrigBright; Lighting.ClockTime=OrigClock; Lighting.FogEnd=OrigFog; Lighting.Ambient=OrigAmbient; Lighting.OutdoorAmbient=OrigOutdoor; Lighting.GlobalShadows=true end

local C={
    BG=Color3.fromRGB(20,12,15),BG2=Color3.fromRGB(30,18,23),PINK=Color3.fromRGB(255,170,195),
    WHITE=Color3.fromRGB(245,235,240),GRAY=Color3.fromRGB(160,140,150),ON=Color3.fromRGB(255,120,160),
    OFF=Color3.fromRGB(55,30,40),RED=Color3.fromRGB(255,40,60),PINK_ESP=Color3.fromRGB(255,130,170),
    YELLOW=Color3.fromRGB(255,200,100),PURPLE=Color3.fromRGB(180,75,255),CYAN=Color3.fromRGB(100,200,255),
    GREEN=Color3.fromRGB(100,255,100),ORANGE=Color3.fromRGB(255,150,50)
}

local Settings={
    Fullbright=false,ShowNPC=false,ShowChanges=false,
    SkinDetect=false,SizeDetect=false,ShowNotify=false,
    Scanner=false,AutoCopy=false,
    Speed=false,Fly=false,Fling=false,Noclip=false,FOV=false,JumpPower=false
}
local MasterOn=true
local ToggleButtons={}

local function SyncAllToggles()
    for _,td in pairs(ToggleButtons)do
        local s=Settings[td.key]
        if s~=nil then td.btn.Text=s and"=^^= ON"or"=;= OFF";td.btn.BackgroundColor3=s and C.ON or C.OFF;td.lbl.TextColor3=s and C.WHITE or C.GRAY end
    end
end

-- PLAYER MODS
local function SetSpeed(v)local c=Player.Character;if c then local h=c:FindFirstChildOfClass("Humanoid");if h then h.WalkSpeed=v and 50 or 16 end end end
local function SetFly(v)local c=Player.Character;if not c then return end;local r=c:FindFirstChild("HumanoidRootPart");if not r then return end;local h=c:FindFirstChildOfClass("Humanoid")
    if v then if h then h.PlatformStand=true end;local bv=Instance.new("BodyVelocity");bv.Velocity=Vector3.zero;bv.MaxForce=Vector3.new(1,1,1)*50000;bv.Parent=r;local bg=Instance.new("BodyGyro");bg.MaxTorque=Vector3.new(1,1,1)*50000;bg.P=3000;bg.Parent=r
        spawn(function()while Settings.Fly and c and c.Parent do local vel=Vector3.zero
            if UserInputService:IsKeyDown(Enum.KeyCode.W)then vel+=Camera.CFrame.LookVector*50 end
            if UserInputService:IsKeyDown(Enum.KeyCode.S)then vel-=Camera.CFrame.LookVector*50 end
            if UserInputService:IsKeyDown(Enum.KeyCode.A)then vel-=Camera.CFrame.RightVector*50 end
            if UserInputService:IsKeyDown(Enum.KeyCode.D)then vel+=Camera.CFrame.RightVector*50 end
            if UserInputService:IsKeyDown(Enum.KeyCode.Space)then vel+=Vector3.new(0,50,0)end
            if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift)then vel-=Vector3.new(0,50,0)end
            if bv then bv.Velocity=vel end;if bg then bg.CFrame=Camera.CFrame end;wait()end end)
    else if r:FindFirstChildOfClass("BodyVelocity")then r:FindFirstChildOfClass("BodyVelocity"):Destroy()end;if r:FindFirstChildOfClass("BodyGyro")then r:FindFirstChildOfClass("BodyGyro"):Destroy()end;if h then h.PlatformStand=false end end end
local function SetFling(v)if v then local c=Player.Character;if c then local r=c:FindFirstChild("HumanoidRootPart");if r then r.Velocity=Vector3.new(0,5000,0)end end;Settings.Fling=false;SyncAllToggles()end end
local function SetNoclip(v)if v then local c=Player.Character;if c then for _,p in ipairs(c:GetDescendants())do if p:IsA("BasePart")then p.CanCollide=false end end end else local c=Player.Character;if c then for _,p in ipairs(c:GetDescendants())do if p:IsA("BasePart")then p.CanCollide=true end end end end end
local function SetFOV(v)Camera.FieldOfView=v and 120 or 70 end
local function SetJump(v)local c=Player.Character;if c then local h=c:FindFirstChildOfClass("Humanoid");if h then h.JumpPower=v and 150 or 50;h.UseJumpPower=true end end end

-- ROOMS
local ROOMS={
    {name="Living Room",min=Vector3.new(-25,0,-25),max=Vector3.new(25,15,25)},
    {name="Garage",min=Vector3.new(25,0,-25),max=Vector3.new(55,15,25)},
    {name="Kitchen",min=Vector3.new(-25,0,25),max=Vector3.new(25,15,55)},
    {name="Basement",min=Vector3.new(-25,-15,-25),max=Vector3.new(25,0,25)},
    {name="Study",min=Vector3.new(-55,0,-25),max=Vector3.new(-25,15,25)},
    {name="Nursery",min=Vector3.new(25,0,25),max=Vector3.new(55,15,55)},
    {name="Master Bedroom",min=Vector3.new(-55,0,25),max=Vector3.new(-25,15,55)},
    {name="Upstairs Hall",min=Vector3.new(-25,8,-55),max=Vector3.new(25,15,-25)},
    {name="Storage Room",min=Vector3.new(25,8,55),max=Vector3.new(55,15,85)},
    {name="Movie Room",min=Vector3.new(-55,8,55),max=Vector3.new(-25,15,85)}
}
local function GetRoom(pos)if not pos then return"?"end;for _,r in ipairs(ROOMS)do if pos.X>=r.min.X and pos.X<=r.max.X and pos.Y>=r.min.Y and pos.Y<=r.max.Y and pos.Z>=r.min.Z and pos.Z<=r.max.Z then return r.name end end;return"?"end

-- ================== PRO ESP ENGINE ==================
local MAX_ESP=60;local SCAN_INTERVAL=0.3;local BASELINE_COOLDOWN=3
local CirclePool={};local TextPool={};local ActiveESP={}
function GetCircle()for i,c in ipairs(CirclePool)do if not c.Visible then return c,i end end;if#CirclePool>=MAX_ESP then return CirclePool[1],1 end;if Drawing then local c=Drawing.new("Circle");c.Thickness=2;c.Filled=false;c.Transparency=1;c.NumSides=32;table.insert(CirclePool,c);return c,#CirclePool end;return nil end
function GetText()for i,t in ipairs(TextPool)do if not t.Visible then return t,i end end;if#TextPool>=MAX_ESP then return TextPool[1],1 end;if Drawing then local t=Drawing.new("Text");t.Size=12;t.Center=true;t.Outline=true;t.OutlineColor=Color3.new();table.insert(TextPool,t);return t,#TextPool end;return nil end
local function IsMonster(obj)if obj:IsA("Model")then if obj:FindFirstChildOfClass("Humanoid")then return true end;if obj:FindFirstChild("HumanoidRootPart")then return true end;if CollectionService:HasTag(obj,"Monster")then return true end;if CollectionService:HasTag(obj,"Enemy")then return true end;if CollectionService:HasTag(obj,"NPC")then return true end end;return false end
local function IsValidTarget(obj)if not obj.Parent then return false end;if obj:IsDescendantOf(Player.Character or workspace)==false then return false end;if obj:IsA("BasePart")and obj.Transparency>0.95 then return false end;if obj.Name=="Terrain"then return false end;if obj:IsA("BasePart")and obj.Anchored and obj.Size.Magnitude<1 then return false end;if obj:IsA("GuiObject")or obj:IsA("ScreenGui")then return false end;return true end
local function GetState(obj)if not IsValidTarget(obj)then return nil end;local s={name=obj.Name};if obj:IsA("BasePart")then s.pos=obj.Position;s.size=obj.Size;s.color=obj.Color;s.material=obj.Material;s.transparency=obj.Transparency elseif obj:IsA("Model")then local pp=obj:FindFirstChildOfClass("BasePart")or obj.PrimaryPart;if pp then s.pos=pp.Position;s.size=pp.Size end end;return s end
local function HasChanged(old,new)if not old or not new then return true end;if old.pos and new.pos then local d=(old.pos-new.pos);if math.abs(d.X)>0.15 or math.abs(d.Y)>0.15 or math.abs(d.Z)>0.15 then return true end end;if old.size and new.size then local d=(old.size-new.size);if math.abs(d.X)>0.15 or math.abs(d.Y)>0.15 or math.abs(d.Z)>0.15 then return true end end;if old.color and new.color and old.color~=new.color then return true end;if old.material and new.material and old.material~=new.material then return true end;if old.transparency and new.transparency and math.abs(old.transparency-new.transparency)>0.02 then return true end;return false end
local BaselineState={};local LiveState={};local ESPData={};local MonsterData={};local MissingData={};local LastBaselineUpdate={};local AnimTime=0

task.spawn(function()while true do task.wait(SCAN_INTERVAL);if not MasterOn then continue end;local now=tick();for obj,baseline in pairs(BaselineState)do if obj and obj.Parent then local live=GetState(obj);if live then LiveState[obj]=live;if HasChanged(baseline,live)then local room=GetRoom(baseline.pos or live.pos);ESPData[obj]={name=obj.Name,pos=live.pos,size=live.size,room=room,type="CHANGED",time=now,color=C.YELLOW}end;if not LastBaselineUpdate[obj]or now-LastBaselineUpdate[obj]>BASELINE_COOLDOWN then BaselineState[obj]=live;LastBaselineUpdate[obj]=now end else if baseline.pos then MissingData[obj]={name=baseline.name,pos=baseline.pos,time=now}end;BaselineState[obj]=nil;LiveState[obj]=nil;ESPData[obj]=nil end end end end)

workspace.DescendantAdded:Connect(function(obj)if not MasterOn then return end;if not(obj:IsA("Model")or obj:IsA("BasePart"))then return end;if not IsValidTarget(obj)then return end;if IsMonster(obj)then local part=obj:IsA("BasePart")and obj or obj:FindFirstChildOfClass("BasePart")or obj.PrimaryPart;if part then MonsterData[obj]={name=obj.Name,pos=part.Position,size=part.Size,time=tick()}end end;local st=GetState(obj);if st then BaselineState[obj]=st;LiveState[obj]=st;LastBaselineUpdate[obj]=tick();local room=GetRoom(st.pos);ESPData[obj]={name=obj.Name,pos=st.pos,size=st.size,room=room,type="NEW",time=tick(),color=C.GREEN}end end)
workspace.DescendantRemoving:Connect(function(obj)if BaselineState[obj]then MissingData[obj]={name=BaselineState[obj].name,pos=BaselineState[obj].pos,time=tick()}end;BaselineState[obj]=nil;LiveState[obj]=nil;ESPData[obj]=nil;MonsterData[obj]=nil end)

RunService.RenderStepped:Connect(function(dt)
    AnimTime+=dt;for _,c in ipairs(CirclePool)do c.Visible=false end;for _,t in ipairs(TextPool)do t.Visible=false end
    if not MasterOn then return end
    local root=Player.Character and Player.Character:FindFirstChild("HumanoidRootPart");local now=tick();local ac=0
    if Settings.ShowNPC and ac<MAX_ESP then for obj,data in pairs(MonsterData)do if obj and obj.Parent and ac<MAX_ESP then local part=obj:IsA("BasePart")and obj or obj:FindFirstChildOfClass("BasePart")or obj.PrimaryPart;if part then data.pos=part.Position;local pos,on=Camera:WorldToViewportPoint(data.pos);if on and root and(data.pos-root.Position).Magnitude<150 then local cr,ci=GetCircle();local txt,ti=GetText();if cr then local r=math.clamp(math.max(part.Size.X,part.Size.Y,part.Size.Z)*0.7,6,35);local room=GetRoom(data.pos);local dist=math.floor((data.pos-root.Position).Magnitude);local vel=part.AssemblyLinearVelocity or part.Velocity or Vector3.zero;local status=vel.Magnitude>0.3 and"ACTIVE"or"IDLE";cr.Position=Vector2.new(pos.X,pos.Y);cr.Radius=r;cr.Color=C.RED;cr.Visible=true;txt.Position=Vector2.new(pos.X,pos.Y-r-14);txt.Text=data.name.."\n"..room.." | "..status.." | "..dist.."m";txt.Color=C.WHITE;txt.Visible=true;ac+=1 end end end end end end
    if Settings.ShowChanges and ac<MAX_ESP then for obj,data in pairs(ESPData)do if obj and obj.Parent and now-data.time<3 and ac<MAX_ESP then if data.pos then local pos,on=Camera:WorldToViewportPoint(data.pos);if on and root and(data.pos-root.Position).Magnitude<100 then local cr,ci=GetCircle();local txt,ti=GetText();if cr then local r=math.clamp(math.max(data.size.X,data.size.Y,data.size.Z)*0.7,5,30);cr.Position=Vector2.new(pos.X,pos.Y);cr.Radius=r;cr.Color=data.color;cr.Visible=true;txt.Position=Vector2.new(pos.X,pos.Y-r-12);txt.Text=data.name.."\n"..data.room.." | "..data.type;txt.Color=C.WHITE;txt.Visible=true;ac+=1 end end end end end end
    if Settings.ShowChanges and ac<MAX_ESP then for obj,data in pairs(MissingData)do if now-data.time<5 and ac<MAX_ESP then if data.pos then local pos,on=Camera:WorldToViewportPoint(data.pos);if on and root and(data.pos-root.Position).Magnitude<100 then local cr,ci=GetCircle();local txt,ti=GetText();if cr then local room=GetRoom(data.pos);cr.Position=Vector2.new(pos.X,pos.Y);cr.Radius=10;cr.Color=C.RED;cr.Visible=true;txt.Position=Vector2.new(pos.X,pos.Y-14);txt.Text="MISSING\n"..room.." | "..data.name;txt.Color=C.RED;txt.Visible=true;ac+=1 end end end end end end
    for obj,data in pairs(ESPData)do if now-data.time>5 then ESPData[obj]=nil end end;for obj,data in pairs(MissingData)do if now-data.time>8 then MissingData[obj]=nil end end
end)

-- ================== NOTIFICATION SYSTEM ==================
local NotifPool={}
local function ShowNotification(title,text,color)
    if not Settings.ShowNotify then return end
    local Frame=Instance.new("Frame");Frame.Size=UDim2.new(0,220,0,50);Frame.Position=UDim2.new(1,-230,0,10);Frame.BackgroundColor3=C.BG2;Frame.BackgroundTransparency=0.2;Frame.BorderSizePixel=0;Frame.Parent=PLAYERGUI;Instance.new("UICorner",Frame).CornerRadius=UDim.new(0,14)
    local Icon=Instance.new("Frame");Icon.Size=UDim2.new(0,4,0,42);Icon.Position=UDim2.new(0,4,0,4);Icon.BackgroundColor3=color or C.GREEN;Icon.BorderSizePixel=0;Icon.Parent=Frame;Instance.new("UICorner",Icon).CornerRadius=UDim.new(1,0)
    local Tl=Instance.new("TextLabel");Tl.Size=UDim2.new(1,-16,0,18);Tl.Position=UDim2.new(0,12,0,4);Tl.Text=title;Tl.TextColor3=color or C.GREEN;Tl.Font=Enum.Font.GothamBold;Tl.TextSize=10;Tl.BackgroundTransparency=1;Tl.TextXAlignment=Enum.TextXAlignment.Left;Tl.Parent=Frame
    local Tx=Instance.new("TextLabel");Tx.Size=UDim2.new(1,-16,0,22);Tx.Position=UDim2.new(0,12,0,22);Tx.Text=text;Tx.TextColor3=C.WHITE;Tx.Font=Enum.Font.Gotham;Tx.TextSize=8;Tx.BackgroundTransparency=1;Tx.TextXAlignment=Enum.TextXAlignment.Left;Tx.Parent=Frame
    table.insert(NotifPool,Frame);if#NotifPool>5 then NotifPool[1]:Destroy();table.remove(NotifPool,1)end;for i,f in ipairs(NotifPool)do f.Position=UDim2.new(1,-230,0,10+(i-1)*55)end;spawn(function()wait(5);if Frame.Parent then TweenService:Create(Frame,TweenInfo.new(0.5),{BackgroundTransparency=1}):Play();wait(0.5);Frame:Destroy()end end)
end

-- ================== SLEEPING CAT (LEZHAT) ==================
local CatArt
local SleepingCatFrames = {
    [[
   ╱|、
  ( -  -)  zZz
   |  ~ |
   じしˍ,)ノ
  =========]],
    [[
   ╱|、
  ( -  -)  zzZ
   |  ~ |
   じしˍ,)ノ
  =========]],
    [[
   ╱|、
  ( =  =)  Zzz
   |  ~ |
   じしˍ,)ノ
  =========]],
    [[
   ╱|、
  ( ˘  ˘)  zZz
   |  ~ |
   じしˍ,)ノ
  =========]],
    [[
   ╱|、
  ( ˚  ˚)  zzZ
   |  ~ |
   じしˍ,)ノ
  =========]]
}

local function StartSleepingCat(parentFrame)
    if CatArt and CatArt.Parent then CatArt:Destroy() end
    
    local CatContainer = Instance.new("Frame")
    CatContainer.Size = UDim2.new(1, 0, 0, 95)
    CatContainer.BackgroundColor3 = C.BG2
    CatContainer.BorderSizePixel = 0
    CatContainer.Parent = parentFrame
    Instance.new("UICorner", CatContainer).CornerRadius = UDim.new(0, 12)
    
    CatArt = Instance.new("TextLabel")
    CatArt.Size = UDim2.new(1, -8, 1, -8)
    CatArt.Position = UDim2.new(0, 4, 0, 4)
    CatArt.Text = SleepingCatFrames[1]
    CatArt.TextColor3 = C.PINK
    CatArt.Font = Enum.Font.Gotham
    CatArt.TextSize = 8
    CatArt.BackgroundTransparency = 1
    CatArt.TextXAlignment = Enum.TextXAlignment.Center
    CatArt.TextYAlignment = Enum.TextYAlignment.Center
    CatArt.Parent = CatContainer
    
    local catIdx = 1
    spawn(function()
        while CatArt and CatArt.Parent do
            catIdx = catIdx % #SleepingCatFrames + 1
            CatArt.Text = SleepingCatFrames[catIdx]
            wait(1.5)
        end
    end)
end

-- ================== GUI ==================
local Gui=Instance.new("ScreenGui");Gui.Name="UwUHub";Gui.Parent=PLAYERGUI;Gui.IgnoreGuiInset=true;Gui.ZIndexBehavior=Enum.ZIndexBehavior.Sibling
local Main,MinIcon

local function CreateMinIcon()
    if MinIcon then MinIcon:Destroy()end
    MinIcon=Instance.new("TextButton");MinIcon.Size=UDim2.new(0,36,0,36);MinIcon.Position=UDim2.new(0.5,-18,0.02,0)
    MinIcon.BackgroundTransparency=1;MinIcon.Text="uwu";MinIcon.TextColor3=C.PINK;MinIcon.Font=Enum.Font.GothamBold
    MinIcon.TextSize=11;MinIcon.BorderSizePixel=0;MinIcon.Active=true;MinIcon.Draggable=true;MinIcon.Parent=Gui
    local bg=Instance.new("Frame");bg.Size=UDim2.new(1,0,1,0);bg.BackgroundColor3=C.BG2;bg.BackgroundTransparency=0.25;bg.BorderSizePixel=0;bg.ZIndex=0;bg.Parent=MinIcon;Instance.new("UICorner",bg).CornerRadius=UDim.new(1,0)
    local glow=Instance.new("Frame");glow.Size=UDim2.new(1,8,1,8);glow.Position=UDim2.new(0,-4,0,-4);glow.BackgroundColor3=C.PINK;glow.BackgroundTransparency=0.7;glow.BorderSizePixel=0;glow.ZIndex=0;glow.Parent=MinIcon;Instance.new("UICorner",glow).CornerRadius=UDim.new(1,0)
    local faces={"uwu","owo",">.<","^-^"};local idx=1
    spawn(function()while MinIcon and MinIcon.Parent do idx=idx%#faces+1;MinIcon.Text=faces[idx];wait(0.6)end end)
    MinIcon.MouseButton1Click:Connect(function()MinIcon:Destroy();MinIcon=nil;CreateMain()end)
end

function CreateMain()
    if Main then Main:Destroy()end;if MinIcon then MinIcon:Destroy();MinIcon=nil end
    ToggleButtons={}
    
    Main=Instance.new("Frame");Main.Size=UDim2.new(0,320,0,310);Main.Position=UDim2.new(0.5,-160,0.5,-155)
    Main.BackgroundColor3=C.BG;Main.BorderSizePixel=0;Main.Active=true;Main.Draggable=true;Main.Parent=Gui
    Instance.new("UICorner",Main).CornerRadius=UDim.new(0,18)
    Main.BackgroundTransparency=1;TweenService:Create(Main,TweenInfo.new(0.3),{BackgroundTransparency=0}):Play()
    
    local Header=Instance.new("Frame");Header.Size=UDim2.new(1,0,0,30);Header.BackgroundColor3=C.BG2;Header.BorderSizePixel=0;Header.Parent=Main;Instance.new("UICorner",Header).CornerRadius=UDim.new(0,18)
    local Title=Instance.new("TextLabel");Title.Size=UDim2.new(0,180,1,0);Title.Position=UDim2.new(0,12,0,0)
    Title.Text="=^^= UWU HUB";Title.TextColor3=C.PINK;Title.Font=Enum.Font.GothamBold;Title.TextSize=12;Title.BackgroundTransparency=1;Title.TextXAlignment=Enum.TextXAlignment.Left;Title.Parent=Header
    local MinBtn=Instance.new("Frame");MinBtn.Size=UDim2.new(0,26,0,26);MinBtn.Position=UDim2.new(1,-32,0.5,-13)
    MinBtn.BackgroundColor3=C.BG2;MinBtn.BorderSizePixel=0;MinBtn.Parent=Header;Instance.new("UICorner",MinBtn).CornerRadius=UDim.new(1,0)
    local MinGlow=Instance.new("Frame");MinGlow.Size=UDim2.new(1,6,1,6);MinGlow.Position=UDim2.new(0,-3,0,-3)
    MinGlow.BackgroundColor3=C.PINK;MinGlow.BackgroundTransparency=0.6;MinGlow.BorderSizePixel=0;MinGlow.ZIndex=0;MinGlow.Parent=MinBtn;Instance.new("UICorner",MinGlow).CornerRadius=UDim.new(1,0)
    local MinText=Instance.new("TextButton");MinText.Size=UDim2.new(1,0,1,0);MinText.Text="-";MinText.BackgroundTransparency=1
    MinText.TextColor3=C.WHITE;MinText.Font=Enum.Font.GothamBold;MinText.TextSize=16;MinText.BorderSizePixel=0;MinText.Parent=MinBtn
    MinText.MouseButton1Click:Connect(function()TweenService:Create(Main,TweenInfo.new(0.25),{Size=UDim2.new(0,0,0,0),Position=UDim2.new(0.5,0,0.5,0),BackgroundTransparency=1}):Play();wait(0.25);Main:Destroy();Main=nil;CreateMinIcon()end)
    
    local TabBar=Instance.new("Frame");TabBar.Size=UDim2.new(1,0,0,22);TabBar.Position=UDim2.new(0,0,0,30);TabBar.BackgroundColor3=C.BG;TabBar.BorderSizePixel=0;TabBar.Parent=Main
    local tabs={"Main","ESP","Player","Detect","Log"};local tabBtns={};local tabFrames={}
    for i,name in ipairs(tabs)do
        local btn=Instance.new("TextButton");btn.Size=UDim2.new(0.2,-2,1,-2);btn.Position=UDim2.new((i-1)*0.2,1,0,1)
        btn.Text=name;btn.BackgroundColor3=C.BG2;btn.TextColor3=C.GRAY;btn.Font=Enum.Font.GothamBold;btn.TextSize=8;btn.BorderSizePixel=0;btn.Parent=TabBar;Instance.new("UICorner",btn).CornerRadius=UDim.new(0,8)
        local frame=Instance.new("ScrollingFrame");frame.Size=UDim2.new(1,-12,1,-58);frame.Position=UDim2.new(0,6,0,56)
        frame.BackgroundTransparency=1;frame.BorderSizePixel=0;frame.ScrollBarThickness=3;frame.ScrollBarImageColor3=C.PINK
        frame.CanvasSize=UDim2.new(0,0,0,0);frame.Visible=false;frame.Parent=Main
        local layout=Instance.new("UIListLayout");layout.SortOrder=Enum.SortOrder.LayoutOrder;layout.Padding=UDim.new(0,3);layout.Parent=frame
        frame.ChildAdded:Connect(function()frame.CanvasSize=UDim2.new(0,0,0,layout.AbsoluteContentSize.Y+10)end)
        tabBtns[name]=btn;tabFrames[name]=frame
        btn.MouseButton1Click:Connect(function()for n,b in pairs(tabBtns)do tabBtns[n].BackgroundColor3=(n==name)and C.PINK or C.BG2;tabBtns[n].TextColor3=(n==name)and C.WHITE or C.GRAY;tabFrames[n].Visible=(n==name)end end)
    end
    tabBtns["Main"].BackgroundColor3=C.PINK;tabBtns["Main"].TextColor3=C.WHITE;tabFrames["Main"].Visible=true
    
    local function Toggle(parent,label,key,default,cb)
        local enabled=Settings[key]~=nil and Settings[key]or default
        local row=Instance.new("Frame");row.Size=UDim2.new(1,0,0,28);row.BackgroundColor3=C.BG2;row.BorderSizePixel=0;row.Parent=parent;Instance.new("UICorner",row).CornerRadius=UDim.new(0,10)
        local lbl=Instance.new("TextLabel");lbl.Size=UDim2.new(0.5,0,1,0);lbl.Position=UDim2.new(0,8,0,0)
        lbl.Text=label;lbl.TextColor3=enabled and C.WHITE or C.GRAY;lbl.Font=Enum.Font.Gotham;lbl.TextSize=9;lbl.BackgroundTransparency=1;lbl.TextXAlignment=Enum.TextXAlignment.Left;lbl.Parent=row
        local btn=Instance.new("TextButton");btn.Size=UDim2.new(0,46,0,22);btn.Position=UDim2.new(1,-52,0.5,-11)
        btn.Text=enabled and"=^^= ON"or"=;= OFF";btn.BackgroundColor3=enabled and C.ON or C.OFF;btn.TextColor3=C.WHITE
        btn.Font=Enum.Font.GothamBold;btn.TextSize=7;btn.BorderSizePixel=0;btn.Parent=row;Instance.new("UICorner",btn).CornerRadius=UDim.new(0,10)
        ToggleButtons[key]={btn=btn,lbl=lbl,key=key}
        btn.MouseButton1Click:Connect(function()enabled=not enabled;Settings[key]=enabled;btn.Text=enabled and"=^^= ON"or"=;= OFF";btn.BackgroundColor3=enabled and C.ON or C.OFF;lbl.TextColor3=enabled and C.WHITE or C.GRAY;cb(enabled)end)
        return row
    end
    
    local mf=tabFrames["Main"]
    local StatusText=Instance.new("TextLabel");StatusText.Size=UDim2.new(1,0,0,16)
    StatusText.Text="=^^= Active";StatusText.TextColor3=C.PINK;StatusText.Font=Enum.Font.GothamBold;StatusText.TextSize=10;StatusText.BackgroundTransparency=1;StatusText.TextXAlignment=Enum.TextXAlignment.Center;StatusText.Parent=mf
    
    local masterRow=Instance.new("Frame");masterRow.Size=UDim2.new(1,0,0,32);masterRow.BackgroundColor3=C.BG2;masterRow.BorderSizePixel=0;masterRow.Parent=mf;Instance.new("UICorner",masterRow).CornerRadius=UDim.new(0,10)
    local masterLbl=Instance.new("TextLabel");masterLbl.Size=UDim2.new(0.5,0,1,0);masterLbl.Position=UDim2.new(0,10,0,0)
    masterLbl.Text="Master Toggle";masterLbl.TextColor3=C.WHITE;masterLbl.Font=Enum.Font.Gotham;masterLbl.TextSize=10;masterLbl.BackgroundTransparency=1;masterLbl.TextXAlignment=Enum.TextXAlignment.Left;masterLbl.Parent=masterRow
    local masterBtn=Instance.new("TextButton");masterBtn.Size=UDim2.new(0,50,0,24);masterBtn.Position=UDim2.new(1,-56,0.5,-12)
    masterBtn.Text=MasterOn and"=^^= ON"or"=;= OFF";masterBtn.BackgroundColor3=MasterOn and C.ON or C.OFF;masterBtn.TextColor3=C.WHITE;masterBtn.Font=Enum.Font.GothamBold;masterBtn.TextSize=8;masterBtn.BorderSizePixel=0;masterBtn.Parent=masterRow;Instance.new("UICorner",masterBtn).CornerRadius=UDim.new(0,10)
    masterBtn.MouseButton1Click:Connect(function()MasterOn=not MasterOn;masterBtn.Text=MasterOn and"=^^= ON"or"=;= OFF";masterBtn.BackgroundColor3=MasterOn and C.ON or C.OFF end)
    
    local ib=Instance.new("Frame");ib.Size=UDim2.new(1,0,0,30);ib.BackgroundColor3=C.BG2;ib.BorderSizePixel=0;ib.Parent=mf;Instance.new("UICorner",ib).CornerRadius=UDim.new(0,10)
    local it=Instance.new("TextLabel");it.Size=UDim2.new(1,-10,1,-10);it.Position=UDim2.new(0,5,0,5)
    it.Text="Anomaly Watch | "..Player.Name.."\nPRO ESP Engine";it.TextColor3=C.WHITE;it.Font=Enum.Font.Gotham;it.TextSize=9;it.BackgroundTransparency=1;it.TextXAlignment=Enum.TextXAlignment.Left;it.TextYAlignment=Enum.TextYAlignment.Top;it.Parent=ib
    
    -- Sleeping Cat
    StartSleepingCat(mf)
    
    Toggle(tabFrames["ESP"],"Fullbright","Fullbright",false,function(v)if v then EnableFB()else DisableFB()end end)
    local l1=Instance.new("TextLabel");l1.Size=UDim2.new(1,0,0,16);l1.Text="MONSTERS (RED)";l1.TextColor3=C.RED;l1.Font=Enum.Font.GothamBold;l1.TextSize=8;l1.BackgroundTransparency=1;l1.TextXAlignment=Enum.TextXAlignment.Left;l1.Parent=tabFrames["ESP"]
    Toggle(tabFrames["ESP"],"Show NPCs","ShowNPC",false,function(v)end)
    local l2=Instance.new("TextLabel");l2.Size=UDim2.new(1,0,0,16);l2.Text="CHANGES (YELLOW/GREEN)";l2.TextColor3=C.YELLOW;l2.Font=Enum.Font.GothamBold;l2.TextSize=8;l2.BackgroundTransparency=1;l2.TextXAlignment=Enum.TextXAlignment.Left;l2.Parent=tabFrames["ESP"]
    Toggle(tabFrames["ESP"],"Show Changes","ShowChanges",false,function(v)end)
    Toggle(tabFrames["ESP"],"Skin Detect","SkinDetect",false,function(v)end)
    Toggle(tabFrames["ESP"],"Size Detect","SizeDetect",false,function(v)end)
    Toggle(tabFrames["ESP"],"Show Notify","ShowNotify",false,function(v)end)
    
    Toggle(tabFrames["Player"],"Speed","Speed",false,function(v)SetSpeed(v)end)
    Toggle(tabFrames["Player"],"Fly","Fly",false,function(v)SetFly(v)end)
    Toggle(tabFrames["Player"],"Fling","Fling",false,function(v)SetFling(v)end)
    Toggle(tabFrames["Player"],"Noclip","Noclip",false,function(v)SetNoclip(v)end)
    Toggle(tabFrames["Player"],"FOV","FOV",false,function(v)SetFOV(v)end)
    Toggle(tabFrames["Player"],"Jump Power","JumpPower",false,function(v)SetJump(v)end)
    
    Toggle(tabFrames["Detect"],"Scanner","Scanner",false,function(v)end)
    Toggle(tabFrames["Detect"],"Auto Copy","AutoCopy",false,function(v)end)
    
    local ls=Instance.new("ScrollingFrame");ls.Size=UDim2.new(1,0,0,170);ls.BackgroundColor3=C.BG2;ls.BorderSizePixel=0
    ls.CanvasSize=UDim2.new(0,0,0,0);ls.ScrollBarThickness=3;ls.ScrollBarImageColor3=C.PINK;ls.Parent=tabFrames["Log"];Instance.new("UICorner",ls).CornerRadius=UDim.new(0,10)
    local ll=Instance.new("UIListLayout");ll.SortOrder=Enum.SortOrder.LayoutOrder;ll.Padding=UDim.new(0,1);ll.Parent=ls
    function AddLog(msg,color)
        local l=Instance.new("TextLabel");l.Size=UDim2.new(1,-4,0,13);l.Text=msg;l.TextColor3=color or C.WHITE;l.Font=Enum.Font.Gotham;l.TextSize=8;l.BackgroundTransparency=1;l.TextXAlignment=Enum.TextXAlignment.Left;l.Parent=ls
        l.BackgroundTransparency=1;TweenService:Create(l,TweenInfo.new(0.2),{BackgroundTransparency=0}):Play()
        ls.CanvasSize=UDim2.new(0,0,0,ll.AbsoluteContentSize.Y+10);ls.CanvasPosition=Vector2.new(0,ls.CanvasSize.Y.Offset)
    end
    AddLog("PRO ESP ENGINE",C.PURPLE)
    AddLog("2-Layer State System",C.CYAN)
    AddLog("Max ESP: "..MAX_ESP.." | Scan: "..SCAN_INTERVAL.."s",C.GRAY)
    
    spawn(function()
        while Main and Main.Parent do wait(1)
            local mc=0;for _ in pairs(MonsterData)do mc=mc+1 end
            local ec=0;for _ in pairs(ESPData)do ec=ec+1 end
            local miss=0;for _ in pairs(MissingData)do miss=miss+1 end
            StatusText.Text=MasterOn and("M:"..mc.." C:"..ec.." X:"..miss)or"=;= Idle"
            StatusText.TextColor3=MasterOn and C.PINK or C.GRAY
        end
    end)
end

CreateMain()
