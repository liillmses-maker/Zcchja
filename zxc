-- UWU HUB HATE WORLD | EVERYTHING WORKS | FINAL

local Players = game:GetService("Players")
local Player = Players.LocalPlayer
local Camera = workspace.CurrentCamera
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local StarterGui = game:GetService("StarterGui")
local Lighting = game:GetService("Lighting")
local PLAYERGUI = Player:WaitForChild("PlayerGui")

-- ================== FULLBRIGHT ==================
local OrigBright = Lighting.Brightness
local OrigClock = Lighting.ClockTime
local OrigFog = Lighting.FogEnd
local OrigAmbient = Lighting.Ambient
local OrigOutdoor = Lighting.OutdoorAmbient

local function EnableFB() Lighting.Brightness = 5; Lighting.ClockTime = 12; Lighting.FogEnd = 99999; Lighting.Ambient = Color3.new(1,1,1); Lighting.OutdoorAmbient = Color3.new(1,1,1); Lighting.GlobalShadows = false end
local function DisableFB() Lighting.Brightness = OrigBright; Lighting.ClockTime = OrigClock; Lighting.FogEnd = OrigFog; Lighting.Ambient = OrigAmbient; Lighting.OutdoorAmbient = OrigOutdoor; Lighting.GlobalShadows = true end

-- ================== COLORS ==================
local C = {
    BG = Color3.new(0.08,0.05,0.06), BG2 = Color3.new(0.12,0.07,0.09),
    ACCENT = Color3.new(1,0.55,0.71), ACCENT2 = Color3.new(0.78,0.27,0.47),
    PINK = Color3.new(1,0.67,0.76), WHITE = Color3.new(0.96,0.92,0.94),
    GRAY = Color3.new(0.63,0.55,0.59), ON = Color3.new(1,0.47,0.63),
    OFF = Color3.new(0.22,0.12,0.18), RED = Color3.new(1,0.16,0.24),
    PINK_ESP = Color3.new(1,0.59,0.71), YELLOW = Color3.new(1,0.78,0.39),
    GREEN = Color3.new(0.39,1,0.39), CYAN = Color3.new(0.39,0.78,1),
    PURPLE = Color3.new(0.7,0.3,1)
}

-- ================== SETTINGS ==================
local Settings = {
    Fullbright = false,
    ShowAnomaly = false,
    ShowObject = false,
    ShowChanges = false,
    SkinDetect = false,
    SizeDetect = false,
    ShowNotify = false,
    Scanner = false,
    AutoCopy = false
}
local MasterOn = false

-- ================== GUI ==================
local Gui = Instance.new("ScreenGui")
Gui.Name = "UwUHub"
Gui.Parent = PLAYERGUI

local Main
local MinimizeIcon

-- Minimize icon
local function CreateMinimizedIcon()
    MinimizeIcon = Instance.new("TextButton")
    MinimizeIcon.Size = UDim2.new(0, 36, 0, 36)
    MinimizeIcon.Position = UDim2.new(0.5, -18, 0.5, -18)
    MinimizeIcon.Text = "uwu"
    MinimizeIcon.BackgroundColor3 = C.BG2
    MinimizeIcon.TextColor3 = C.PINK
    MinimizeIcon.Font = Enum.Font.GothamBold
    MinimizeIcon.TextSize = 10
    MinimizeIcon.BorderSizePixel = 0
    MinimizeIcon.Active = true
    MinimizeIcon.Draggable = true
    MinimizeIcon.Parent = Gui
    Instance.new("UICorner", MinimizeIcon).CornerRadius = UDim.new(1, 0)
    
    local faces = {"uwu", "owo", ">.<", "^-^"}
    local idx = 1
    spawn(function()
        while MinimizeIcon and MinimizeIcon.Parent do
            idx = idx % #faces + 1
            MinimizeIcon.Text = faces[idx]
            wait(0.6)
        end
    end)
    
    MinimizeIcon.MouseButton1Click:Connect(function()
        MinimizeIcon:Destroy()
        MinimizeIcon = nil
        CreateMainGUI()
    end)
end

-- Main GUI
function CreateMainGUI()
    if Main then Main:Destroy() end
    if MinimizeIcon then MinimizeIcon:Destroy(); MinimizeIcon = nil end
    
    Main = Instance.new("Frame")
    Main.Size = UDim2.new(0, 300, 0, 240)
    Main.Position = UDim2.new(0.5, -150, 0.5, -120)
    Main.BackgroundColor3 = C.BG
    Main.BorderSizePixel = 0
    Main.Active = true
    Main.Draggable = true
    Main.Parent = Gui
    Instance.new("UICorner", Main).CornerRadius = UDim.new(0, 20)
    
    -- Header
    local Header = Instance.new("Frame")
    Header.Size = UDim2.new(1, 0, 0, 26)
    Header.BackgroundColor3 = C.BG2
    Header.BorderSizePixel = 0
    Header.Parent = Main
    Instance.new("UICorner", Header).CornerRadius = UDim.new(0, 20)
    
    local Title = Instance.new("TextLabel")
    Title.Size = UDim2.new(0, 100, 1, 0)
    Title.Position = UDim2.new(0, 10, 0, 0)
    Title.Text = "UWU HUB"
    Title.TextColor3 = C.PINK
    Title.Font = Enum.Font.GothamBold
    Title.TextSize = 10
    Title.BackgroundTransparency = 1
    Title.TextXAlignment = Enum.TextXAlignment.Left
    Title.Parent = Header
    
    local CloseBtn = Instance.new("TextButton")
    CloseBtn.Size = UDim2.new(0, 20, 0, 20)
    CloseBtn.Position = UDim2.new(1, -40, 0.5, -10)
    CloseBtn.Text = "-"
    CloseBtn.BackgroundColor3 = C.OFF
    CloseBtn.TextColor3 = C.PINK
    CloseBtn.Font = Enum.Font.GothamBold
    CloseBtn.TextSize = 12
    CloseBtn.BorderSizePixel = 0
    CloseBtn.Parent = Header
    Instance.new("UICorner", CloseBtn).CornerRadius = UDim.new(1, 0)
    CloseBtn.MouseButton1Click:Connect(function()
        Main:Destroy()
        Main = nil
        CreateMinimizedIcon()
    end)
    
    -- Master toggle
    local MTog = Instance.new("Frame")
    MTog.Size = UDim2.new(0, 32, 0, 16)
    MTog.Position = UDim2.new(1, -36, 0.5, -8)
    MTog.BackgroundColor3 = C.OFF
    MTog.BorderSizePixel = 0
    MTog.Parent = Header
    Instance.new("UICorner", MTog).CornerRadius = UDim.new(1, 0)
    
    local MKnob = Instance.new("Frame")
    MKnob.Size = UDim2.new(0, 12, 0, 12)
    MKnob.Position = UDim2.new(0, 2, 0.5, -6)
    MKnob.BackgroundColor3 = C.WHITE
    MKnob.BorderSizePixel = 0
    MKnob.Parent = MTog
    Instance.new("UICorner", MKnob).CornerRadius = UDim.new(1, 0)
    
    local MFace = Instance.new("TextLabel")
    MFace.Size = UDim2.new(1, 0, 1, 0)
    MFace.Text = "=;="
    MFace.TextColor3 = C.GRAY
    MFace.Font = Enum.Font.GothamBold
    MFace.TextSize = 7
    MFace.BackgroundTransparency = 1
    MFace.Parent = MKnob
    
    local function UpdateMaster(s)
        MasterOn = s
        if s then
            TweenService:Create(MTog, TweenInfo.new(0.2), {BackgroundColor3 = C.ON}):Play()
            TweenService:Create(MKnob, TweenInfo.new(0.2), {Position = UDim2.new(1, -14, 0.5, -6)}):Play()
            MFace.Text = "=^^="; MFace.TextColor3 = C.ON
        else
            TweenService:Create(MTog, TweenInfo.new(0.2), {BackgroundColor3 = C.OFF}):Play()
            TweenService:Create(MKnob, TweenInfo.new(0.2), {Position = UDim2.new(0, 2, 0.5, -6)}):Play()
            MFace.Text = "=;="; MFace.TextColor3 = C.GRAY
        end
    end
    MTog.InputBegan:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
            UpdateMaster(not MasterOn)
        end
    end)
    
    -- Tabs
    local TabBar = Instance.new("Frame")
    TabBar.Size = UDim2.new(1, 0, 0, 20)
    TabBar.Position = UDim2.new(0, 0, 0, 26)
    TabBar.BackgroundColor3 = C.BG
    TabBar.BorderSizePixel = 0
    TabBar.Parent = Main
    
    local tabs = {"Main", "ESP", "Detect", "Log"}
    local tabBtns = {}
    local tabFrames = {}
    
    for i, name in ipairs(tabs) do
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(0.25, -1, 1, -1)
        btn.Position = UDim2.new((i-1)*0.25, 0, 0, 0)
        btn.Text = name
        btn.BackgroundColor3 = C.BG2
        btn.TextColor3 = C.GRAY
        btn.Font = Enum.Font.GothamBold
        btn.TextSize = 9
        btn.BorderSizePixel = 0
        btn.Parent = TabBar
        Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 10)
        
        local frame = Instance.new("Frame")
        frame.Size = UDim2.new(1, -10, 1, -52)
        frame.Position = UDim2.new(0, 5, 0, 50)
        frame.BackgroundTransparency = 1
        frame.Visible = false
        frame.Parent = Main
        
        local layout = Instance.new("UIListLayout")
        layout.SortOrder = Enum.SortOrder.LayoutOrder
        layout.Padding = UDim.new(0, 3)
        layout.Parent = frame
        
        tabBtns[name] = btn
        tabFrames[name] = frame
        
        btn.MouseButton1Click:Connect(function()
            for n, b in pairs(tabBtns) do
                if n == name then
                    b.BackgroundColor3 = C.ACCENT2
                    b.TextColor3 = C.WHITE
                    tabFrames[n].Visible = true
                else
                    b.BackgroundColor3 = C.BG2
                    b.TextColor3 = C.GRAY
                    tabFrames[n].Visible = false
                end
            end
        end)
    end
    
    tabBtns["Main"].BackgroundColor3 = C.ACCENT2
    tabBtns["Main"].TextColor3 = C.WHITE
    tabFrames["Main"].Visible = true
    
    -- ================== PAW TOGGLE FUNCTION ==================
    local function MakePawToggle(parent, label, default, callback)
        local enabled = default
        local row = Instance.new("Frame")
        row.Size = UDim2.new(1, 0, 0, 32)
        row.BackgroundColor3 = C.BG2
        row.BorderSizePixel = 0
        row.Parent = parent
        Instance.new("UICorner", row).CornerRadius = UDim.new(0, 12)
        
        local lbl = Instance.new("TextLabel")
        lbl.Size = UDim2.new(0.5, 0, 1, 0)
        lbl.Position = UDim2.new(0, 8, 0, 0)
        lbl.Text = label
        lbl.TextColor3 = C.GRAY
        lbl.Font = Enum.Font.Gotham
        lbl.TextSize = 10
        lbl.BackgroundTransparency = 1
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.Parent = row
        
        local pawBtn = Instance.new("TextButton")
        pawBtn.Size = UDim2.new(0, 40, 0, 40)
        pawBtn.Position = UDim2.new(1, -46, 0.5, -20)
        pawBtn.Text = "=;="
        pawBtn.BackgroundColor3 = C.OFF
        pawBtn.TextColor3 = C.GRAY
        pawBtn.Font = Enum.Font.GothamBold
        pawBtn.TextSize = 10
        pawBtn.BorderSizePixel = 0
        pawBtn.Parent = row
        Instance.new("UICorner", pawBtn).CornerRadius = UDim.new(1, 0)
        
        local function SetState(state)
            enabled = state
            if state then
                pawBtn.Text = "=^^="
                TweenService:Create(pawBtn, TweenInfo.new(0.2), {BackgroundColor3 = C.ON, TextColor3 = C.WHITE}):Play()
                lbl.TextColor3 = C.WHITE
            else
                pawBtn.Text = "=;="
                TweenService:Create(pawBtn, TweenInfo.new(0.2), {BackgroundColor3 = C.OFF, TextColor3 = C.GRAY}):Play()
                lbl.TextColor3 = C.GRAY
            end
        end
        
        pawBtn.MouseButton1Click:Connect(function()
            SetState(not enabled)
            callback(not enabled)
        end)
        
        SetState(default)
        return row
    end
    
    -- ================== MAIN TAB ==================
    local mainFrame = tabFrames["Main"]
    
    local info = Instance.new("Frame")
    info.Size = UDim2.new(1, 0, 0, 50)
    info.BackgroundColor3 = C.BG2
    info.BorderSizePixel = 0
    info.Parent = mainFrame
    Instance.new("UICorner", info).CornerRadius = UDim.new(0, 12)
    
    local infoText = Instance.new("TextLabel")
    infoText.Size = UDim2.new(1, -10, 1, -10)
    infoText.Position = UDim2.new(0, 5, 0, 5)
    infoText.Text = "Game: Anomaly Watch\nPlayer: " .. Player.Name .. "\nUWU HUB HATE WORLD"
    infoText.TextColor3 = C.WHITE
    infoText.Font = Enum.Font.Gotham
    infoText.TextSize = 10
    infoText.BackgroundTransparency = 1
    infoText.TextXAlignment = Enum.TextXAlignment.Left
    infoText.TextYAlignment = Enum.TextYAlignment.Top
    infoText.Parent = info
    
    local catBox = Instance.new("Frame")
    catBox.Size = UDim2.new(1, 0, 0, 80)
    catBox.BackgroundColor3 = C.BG2
    catBox.BorderSizePixel = 0
    catBox.Parent = mainFrame
    Instance.new("UICorner", catBox).CornerRadius = UDim.new(0, 12)
    
    local catArt = Instance.new("TextLabel")
    catArt.Size = UDim2.new(1, -6, 1, -6)
    catArt.Position = UDim2.new(0, 3, 0, 3)
    catArt.Text = [[  ╱|、
(˚ˎ 。7  
 |、˜〵          
じしˍ,)ノ
   zzz]]
    catArt.TextColor3 = C.PINK
    catArt.Font = Enum.Font.Gotham
    catArt.TextSize = 9
    catArt.BackgroundTransparency = 1
    catArt.TextXAlignment = Enum.TextXAlignment.Center
    catArt.TextYAlignment = Enum.TextYAlignment.Center
    catArt.Parent = catBox
    
    local catFrames = {
        [[  ╱|、
(˚ˎ 。7  
 |、˜〵          
じしˍ,)ノ
   zzz]],
        [[  ╱|、
( -ˎ -)  
 |、˜〵          
じしˍ,)ノ
  zzZ]],
        [[  ╱|、
( =ˎ =)  
 |、˜〵          
じしˍ,)ノ
  zZz]],
        [[  ╱|、
( ˘ˎ ˘)  
 |、˜〵          
じしˍ,)ノ
   zzz]]
    }
    local catIdx = 1
    spawn(function()
        while catArt and catArt.Parent do
            catIdx = catIdx % #catFrames + 1
            catArt.Text = catFrames[catIdx]
            wait(1.2)
        end
    end)
    
    local statusLabel = Instance.new("TextLabel")
    statusLabel.Size = UDim2.new(1, 0, 0, 14)
    statusLabel.Text = "uwu zzz..."
    statusLabel.TextColor3 = C.GRAY
    statusLabel.Font = Enum.Font.Gotham
    statusLabel.TextSize = 9
    statusLabel.BackgroundTransparency = 1
    statusLabel.TextXAlignment = Enum.TextXAlignment.Center
    statusLabel.Parent = mainFrame
    
    -- ================== ESP TAB ==================
    MakePawToggle(tabFrames["ESP"], "Fullbright", false, function(v) Settings.Fullbright = v; if v then EnableFB() else DisableFB() end end)
    MakePawToggle(tabFrames["ESP"], "Show Anomaly", false, function(v) Settings.ShowAnomaly = v end)
    MakePawToggle(tabFrames["ESP"], "Show Object", false, function(v) Settings.ShowObject = v end)
    MakePawToggle(tabFrames["ESP"], "Show Changes", false, function(v) Settings.ShowChanges = v end)
    MakePawToggle(tabFrames["ESP"], "Skin Detect", false, function(v) Settings.SkinDetect = v end)
    MakePawToggle(tabFrames["ESP"], "Size Detect", false, function(v) Settings.SizeDetect = v end)
    MakePawToggle(tabFrames["ESP"], "Show Notify", false, function(v) Settings.ShowNotify = v end)
    
    -- ================== DETECT TAB ==================
    MakePawToggle(tabFrames["Detect"], "Scanner", false, function(v) Settings.Scanner = v end)
    MakePawToggle(tabFrames["Detect"], "Auto Copy", false, function(v) Settings.AutoCopy = v end)
    
    -- ================== LOG TAB ==================
    local logScroll = Instance.new("ScrollingFrame")
    logScroll.Size = UDim2.new(1, 0, 0, 145)
    logScroll.BackgroundColor3 = C.BG2
    logScroll.BorderSizePixel = 0
    logScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
    logScroll.ScrollBarThickness = 3
    logScroll.ScrollBarImageColor3 = C.ACCENT
    logScroll.Parent = tabFrames["Log"]
    Instance.new("UICorner", logScroll).CornerRadius = UDim.new(0, 10)
    
    local logLayout = Instance.new("UIListLayout")
    logLayout.SortOrder = Enum.SortOrder.LayoutOrder
    logLayout.Padding = UDim.new(0, 1)
    logLayout.Parent = logScroll
    
    local logCount = 0
    function _G.UWU_AddLog(msg, color)
        logCount = logCount + 1
        local lbl = Instance.new("TextLabel")
        lbl.Size = UDim2.new(1, -4, 0, 12)
        lbl.Text = msg
        lbl.TextColor3 = color or C.WHITE
        lbl.Font = Enum.Font.Gotham
        lbl.TextSize = 8
        lbl.BackgroundTransparency = 1
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.Parent = logScroll
        logScroll.CanvasSize = UDim2.new(0, 0, 0, logLayout.AbsoluteContentSize.Y + 2)
        logScroll.CanvasPosition = Vector2.new(0, logScroll.CanvasSize.Y.Offset)
    end
    _G.UWU_AddLog("UWU HUB HATE WORLD", C.PURPLE)
    _G.UWU_AddLog("Ready", C.GRAY)
    
    -- ================== UPDATE STATUS ==================
    spawn(function()
        while Main and Main.Parent do
            if MasterOn then
                statusLabel.Text = "uwu on"
                statusLabel.TextColor3 = C.PINK
            else
                statusLabel.Text = "uwu zzz..."
                statusLabel.TextColor3 = C.GRAY
            end
            wait(1)
        end
    end)
end

-- ================== ESP SYSTEM ==================
local ANOMALY_NAMES = {"Alternate","HallwayIntruder","Devil","Mask","Dummy","Mannequin","BloodPart","Pentagram","Eyes","Jumpscares","N"}
local OBJECT_NAMES = {"AnnoyingOrange","NormalOrange","Orange","Float Duck","Duck","Crib","Rockingmachine","Bar Soap","Soap","Mirror","Sink","Curtain","Bucket","TV","Telephone","Alarm Clock","WallClock","ClockFace","Whiteboard","To-Do-List","StickyNotes","Papers","Paper","Books","Open Book","Poster","Painting","MonaLisa","WallDrawing","Frame","Monitor","Screen","Keyboard","Mouse","Carpet","Chair","Table","RoundTable","Pool Table","Desk","Shelf","Shoe Rack","Shoes","Wardrobe","Closet","Door","Window","Fridge","Oven","Frying Pan","Cup","g Cup","CoffeeJar","SugarJar","Container","Cola","ToiletPaperRoll","ToiletPlunger","Trash Can","Trash Bag","Suitcase","Briefcase","Box","Cardboard","Tire","Plant","Vase","Light","Lamp","Neon","Figurine","Bed","Pillow","Glass","Bleach","crayon","pic1","pic2","pic3","Projector"}

local Detected = {}
local Circles = {}
local LastScan = 0
local AnimTime = 0

function GetCircle()
    for _, c in ipairs(Circles) do if not c.Visible then return c end end
    local c = Drawing.new("Circle"); c.Thickness = 2; c.Filled = false; c.Transparency = 1; c.NumSides = 32
    table.insert(Circles, c); return c
end

local function InList(name, list)
    for _, k in ipairs(list) do if name == k then return true end end
    return false
end

RunService.RenderStepped:Connect(function(dt)
    AnimTime = AnimTime + dt
    
    if MasterOn and (Settings.ShowAnomaly or Settings.ShowObject or Settings.ShowChanges) then
        LastScan = LastScan + dt
        if LastScan >= 0.3 then
            LastScan = 0; Detected = {}
            for _, obj in ipairs(workspace:GetDescendants()) do
                if not (obj:IsA("Model") or obj:IsA("BasePart")) then continue end
                local ok = false; local col = C.PINK_ESP
                
                if Settings.ShowAnomaly and InList(obj.Name, ANOMALY_NAMES) then ok = true; col = C.RED
                elseif Settings.ShowObject and InList(obj.Name, OBJECT_NAMES) then ok = true; col = C.PINK_ESP
                elseif Settings.ShowChanges then
                    local p = obj:IsA("BasePart") and obj or obj:FindFirstChildOfClass("BasePart")
                    if p and p:IsA("BasePart") and (p.AssemblyLinearVelocity.Magnitude > 0.1 or p.RotVelocity.Magnitude > 0.1) then
                        ok = true; col = C.YELLOW
                    end
                end
                
                if ok then
                    local r = 5
                    if obj:IsA("BasePart") then r = math.clamp(math.max(obj.Size.X, obj.Size.Y, obj.Size.Z) * 0.7, 4, 35)
                    elseif obj:IsA("Model") then
                        local pp = obj:FindFirstChildOfClass("BasePart") or obj.PrimaryPart
                        if pp then r = math.clamp(math.max(pp.Size.X, pp.Size.Y, pp.Size.Z) * 0.7, 4, 35) end
                    end
                    Detected[obj] = {col = col, r = r}
                end
            end
        end
        
        for _, c in ipairs(Circles) do c.Visible = false end
        for obj, data in pairs(Detected) do
            if obj and obj.Parent then
                local part = obj:IsA("BasePart") and obj or obj:FindFirstChildOfClass("BasePart")
                if part then
                    local pos, onScreen = Camera:WorldToViewportPoint(part.Position)
                    if onScreen then
                        local cr = GetCircle()
                        if cr then
                            local seed = string.byte(obj.Name, 1) or 1
                            cr.Position = Vector2.new(pos.X, pos.Y)
                            cr.Radius = data.r * (1 + math.sin(AnimTime * 2 + seed) * 0.04)
                            cr.Color = data.col
                            cr.Visible = true
                        end
                    end
                end
            end
        end
    else
        for _, c in ipairs(Circles) do c.Visible = false end
    end
end)

-- ================== START ==================
CreateMainGUI()
